name: Award Flight Monitor

on:
  schedule:
    # Every 30 min from 09:00 to 22:00 UTC (06:00-19:00 BRT)
    - cron: '*/30 9-22 * * *'
  workflow_dispatch: # allow manual trigger

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Restore SQLite state
        uses: actions/cache@v4
        with:
          path: data/state.sqlite
          key: sqlite-state-${{ github.run_number }}
          restore-keys: |
            sqlite-state-

      - name: Run monitor
        env:
          GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
          GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
          GMAIL_REFRESH_TOKEN: ${{ secrets.GMAIL_REFRESH_TOKEN }}
          GMAIL_USER_EMAIL: ${{ secrets.GMAIL_USER_EMAIL }}
          SEATS_AERO_LABEL: seats-aero/real-alerts
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          NODE_ENV: production
          LOG_LEVEL: info
          DB_PATH: ./data/state.sqlite
        run: npm run search

      - name: Save SQLite state
        uses: actions/cache@v4
        if: always()
        with:
          path: data/state.sqlite
          key: sqlite-state-${{ github.run_number }}
